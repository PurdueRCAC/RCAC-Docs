<!-- Note: generated by tools/generate_agent_context.py from tools/agent_context/clusters/gilbreth.yml. Manual changes will be lost! -->
# Slurm Scheduler (Gilbreth)

All real computation on Gilbreth runs as **Slurm** jobs. Never run compute on a login
node (see `unix.md`) — submit it here.

## Every Gilbreth job needs four things

A job request must specify **resources**, an **account**, a **QOS**, and a
**partition**. On Gilbreth you MUST explicitly set **both the account
(`-A`/`--account=`) and the partition (`-p`/`--partition=`)**; also set the QOS
(`-q`/`--qos=`) and always a time limit (`--time`).

- Discover the accounts you can charge (and their balances) with **`slist`** — do not
  guess an account name.
- Set a realistic `--time`; memory is allocated in proportion to the cores you request.

## Partitions

| Partition | For | Notes |
|-----------|-----|-------|
| `a10` | A10 GPU jobs (3× A10 24 GB per node) | QOS `normal` or `standby`; up to 2 weeks (`standby` max 4 h) |
| `a30` | A30 GPU jobs (3× A30 24 GB per node) | QOS `normal` or `standby`; up to 2 weeks (`standby` max 4 h) |
| `a100-40gb` | A100-40GB GPU jobs (2–4× A100 40 GB per node) | QOS `normal` or `standby`; up to 2 weeks; NVLink nodes need `--constraint=nvlink` |
| `a100-80gb` | A100-80GB GPU jobs (2× A100 80 GB per node) | QOS `normal` or `standby`; up to 2 weeks (`standby` max 4 h) |
| `training` | Shared multi-GPU training pool (4 GPUs/node; V100/A100/H100) | QOS `training` only; up to 24 h; for demonstrated >2-GPU workflows |

## QOS and charging

- **`normal`** — the default; normal priority, charged against your account.
- **`standby`** — access to idle resources at low priority, **not charged**, max 4
  hours. Request it with **`-q standby`** (it is a QOS, **not** an account).
- **`training`** — required for the `training` partition (`-q training`); low priority,
  **not charged**, max 24 hours.

## Common commands

`sbatch` (submit a batch script), `squeue -u $USER` (your jobs), `scontrol show job
<id>` (job detail), `scontrol hold`/`release`, `scancel <id>` (cancel), `sinteractive`
(interactive job), `srun` (launch tasks).

## Prohibitions

- **Do NOT** assume a `preemptible` QOS — Gilbreth has none; the low-priority QOS is `standby`, and the shared multi-GPU pool uses `-q training`.
- **Do NOT** pick a GPU type with a `--gres` type string — on Gilbreth the GPU type is chosen by **partition** (`a10`/`a30`/`a100-40gb`/`a100-80gb`); request a count with `--gpus-per-node=N`.
- **Do NOT** write `-A standby` or `#SBATCH -A standby` — `standby` is a QOS
  (`-q standby`), not an account.
- **Do NOT** emit flags for other schedulers (PBS/Torque `#PBS`, LSF `#BSUB`) — this is
  Slurm.
- **Do NOT** invent partition or QOS names; the valid sets are listed above.
- **Do NOT** submit jobs without a `--time` limit, and **do NOT** `scancel` jobs you do
  not own or run bulk cancellations without confirming with the user.
